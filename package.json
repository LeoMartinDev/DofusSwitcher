{
  "name": "node-native-addons-example",
  "version": "1.0.0",
  "dependencies": {

  },
  "scripts": {
    "rebuild": "node-gyp rebuild",
    "build": "node-gyp build --target=1.7.3 --target_platform=win32 --arch=x64",
    "configure": "node-gyp configure",
    "clean": "node-gyp clean",
    "node-gyp:rebuild": "node-gyp rebuild --target=1.7.3 --target_platform=win32 --arch=x64 --dist-url=https://atom.io/download/electron",
    "electron:rebuild": "./node_modules/.bin/electron-rebuild --target=1.7.3 --target_platform=win32 --arch=x64",
    "electron:start": "./node_modules/.bin/electron main.js",
    "start": "npm run electron:rebuild && npm run electron:start"
  },
  "gypfile": true,
  "devDependencies": {
    "bindings": "^1.3.0",
    "electron-rebuild": "^1.7.3",
    "electron": "^1.8.3",
    "nan": "^2.6.1",
    "node-gyp": "^3.6.0"
  }
}
